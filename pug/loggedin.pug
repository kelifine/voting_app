doctype html
html
  include head.pug
  
block header
    nav(class="navbar navbar-default")
        div(class="container-fluid")
        p(class="navbar-text")
            a(href="/") Voting App
        a(href="/logout")
            button(type="button" class="btn btn-default navbar-btn navbar-right" id="logout") Log Out 
                image(src="/img/github.png")
        ul(class="nav navbar-nav navbar-right")
            li
                a(href="/createChart" class="polls" id="newpoll") New Poll
            li
                a(href="/myPolls" class="polls" id="mypolls") My Polls
    h1(class="text-center" id="welcome") #{locals.welcome}           

